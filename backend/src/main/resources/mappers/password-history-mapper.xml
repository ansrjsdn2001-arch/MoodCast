<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moodcast.member.dao.PasswordHistoryDao">

    <select id="findRecentPasswordHashes" resultType="String">
        select password_hash
        from member_password_history
        where member_id = #{memberId}
        order by created_at desc
        limit #{limit}
    </select>

    <insert id="insertPasswordHistory">
        insert into member_password_history (
            member_id,
            password_hash
        ) values (
            #{memberId},
            #{passwordHash}
        )
    </insert>

</mapper>
